android {
            defaultConfig {
                applicationId = "org.nativescript.upcomingg"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }