android {
            defaultConfig {
                applicationId = "org.nativescript.Groceries"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }